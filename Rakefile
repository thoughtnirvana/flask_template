namespace :deps do
  desc "Install dependencies"
  task :install do
    sh "pip install -r reqs"
  end

  desc "Upgrade dependencies"
  task :upgrade do
    sh "pip install -r reqs --upgrade"
  end
end
